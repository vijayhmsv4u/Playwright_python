stages:
  - test
  - quality
  - docker

# ------------------------------------------------
# GLOBAL VARIABLES
# ------------------------------------------------
variables:
  PLAYWRIGHT_BROWSERS_PATH: "/ms-playwright"
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip

# ------------------------------------------------
# PLAYWRIGHT TEST + ALLURE REPORT
# ------------------------------------------------
playwright_tests:
  stage: test
  image: mcr.microsoft.com/playwright/python:v1.58.0-jammy

  before_script:
    - python --version
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - playwright install --with-deps

  script:
    - pytest tests --alluredir=allure-results

  artifacts:
    when: always
    paths:
      - allure-results
      - reports
    expire_in: 1 week

# ------------------------------------------------
# SONARCLOUD ANALYSIS
# ------------------------------------------------
sonarqube_check:
  stage: quality
  image:
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]

  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"

  cache:
    key: sonar
    paths:
      - .sonar/cache

  script:
    - sonar-scanner
        -Dsonar.projectKey=vijayhmsv93-project
        -Dsonar.organization=vijay-bhaskar
        -Dsonar.sources=.
        -Dsonar.host.url=$SONAR_HOST_URL
        -Dsonar.login=$SONAR_TOKEN

  allow_failure: false

# ------------------------------------------------
# DOCKER BUILD STAGE
# ------------------------------------------------
docker_build:
  stage: docker
  image: docker:latest
  services:
    - docker:dind

  variables:
    DOCKER_TLS_CERTDIR: ""

  before_script:
    - docker info

  script:
    - docker build -t playwright-python-project .

  only:
    - main